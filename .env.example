# Sample configuration for HomeMatch-AI
# Copy this file to `.env` and adjust values if needed

LISTINGS_CSV=listings.csv
VECTOR_DB_DIR=chroma_db
CACHE_FILE=listings_cache.pkl
IMAGES_DIR=images
DEFAULT_IMAGE=default_image.png
TOP_K=5
